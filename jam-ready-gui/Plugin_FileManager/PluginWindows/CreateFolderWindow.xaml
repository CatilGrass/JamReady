<Window x:Class="Plugin_FileManager.PluginWindows.CreateFolderWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:Plugin_FileManager.PluginWindows"
        Style="{StaticResource CommonWindowStyle}"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        mc:Ignorable="d"
        Title="New Folder" Height="220" Width="320">
    <Grid Margin="25">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <TextBox 
            Grid.Row="0" 
            Name="NameInput" 
            Text="New Folder"
            Style="{StaticResource JamReadyTextBoxStyleTransparent}"
            FontSize="17"
            TextAlignment="Center"
            VerticalContentAlignment="Center"
            />
        
        <Button 
            Grid.Row="1"
            Name="CreateButton"
            Click="CreateButton_OnClick"
            Style="{StaticResource JamReadyButtonStyle}"
            Margin="15" 
            Content="Create"/>
        
    </Grid>
</Window>
